import notebookReducer, {addNote, updateNote, deleteNote} from './note';
import settingsReducer, {
  setDarkTheme,
  setHelperText,
  setUsername,
  setLanguage,
  setLastExport,
  setLastImport,
} from './settings';

export {
  notebookReducer,
  settingsReducer,
  addNote,
  updateNote,
  deleteNote,
  setDarkTheme,
  setHelperText,
  setLanguage,
  setUsername,
  setLastExport,
  setLastImport,
};

import React from 'react';
import { Text, View } from 'react-native';

const Write = () => {
  return (
    <View style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
      }}>
      <Text>Write</Text>
    </View>
  );
}

export default Write;
import React from 'react';
import { Text, View } from 'react-native';

const Ready = () => {
  return (
    <View style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
      }}>
      <Text>Ready</Text>
    </View>
  );
}

export default Ready;